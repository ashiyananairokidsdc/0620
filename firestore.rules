rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 認証なしで利用するため、responses コレクションの読み書きを許可します。
    // （個人情報を含むため、公開URLを不特定多数に共有しないでください）
    match /responses/{docId} {
      allow read, write: if true;
    }
  }
}
