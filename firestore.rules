rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 個人情報（氏名・連絡先）を含むため、ログイン済みユーザーのみ読み書き可。
    // 本番では下のように必ず認証必須にしてください（テストモードのまま公開しない）。
    match /responses/{docId} {
      allow read, write: if request.auth != null;
    }
  }
}
